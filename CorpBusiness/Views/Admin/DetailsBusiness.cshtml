@model CorpBusiness.Models.Vendor

@{
    ViewBag.Title = "DetailsBusiness";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>DetailsBusiness</h2>

<fieldset>
    <legend>Vendor</legend>

    <div class="display-label">BusinessTitles</div>
    <div class="display-field">@Model.BusinessTitles</div>

    <div class="display-label">CompanyName</div>
    <div class="display-field">@Model.CompanyName</div>

    <div class="display-label">Category</div>
    <div class="display-field">@Model.Catgry</div>

    <div class="display-label">Sub Category</div>
    <div class="display-field">@Model.SubCatgry</div>

    <div class="display-label">Email</div>
    <div class="display-field">@Model.Email</div>

    <div class="display-label">Name</div>
    <div class="display-field">@Model.Name</div>

    <div class="display-label">Address1</div>
    <div class="display-field">@Model.Address1</div>

    <div class="display-label">Address2</div>
    <div class="display-field">@Model.Address2</div>

    <div class="display-label">City</div>
    <div class="display-field">@Model.City</div>

    <div class="display-label">State</div>
    <div class="display-field">@Model.State</div>

    <div class="display-label">Pin</div>
    <div class="display-field">@Model.Pin</div>

    <div class="display-label">Country</div>
    <div class="display-field">@Model.Country</div>

    <div class="display-label">Phone</div>
    <div class="display-field">@Model.Phone</div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "EditBusiness", new { id=Model.Id }) |
    @Html.ActionLink("Back to List", "Business")
</p>

